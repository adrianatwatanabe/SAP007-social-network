

FAZER:
  Posts (Criar e Mostrar na Tela):
    - Função que valide a criação de postagem ao clicar em enviar no modal 
    - Função que receba e atribua os valores para o post que será mostrado na página FEED
    - Retirar o template da parte de Add Comentário (input de adicionar comentário) (para a página de FEED  e PROFILE)
    - Retirar o template de mostrar TODOS OS COMENTÁRIOS EM CADA POST (para a página de FEED  e PROFILE)
    - Fechar o modal usando a função de fechar encontrado do arquivo modal.js
  
  Posts (Deletar e Editar):
    - Criar html que confirme a exclusão do post
    - Criar html que edite cada postagem do próprio usuário logado 

  Curtir:
    - Estudar e escrever os babys steps da ação de CLICAR E DESCLICAR NO LIKE

  - Fazer uma página de erro 404 para links e páginas que não serão mostradas 

___________________________________________________________

FAZENDO:
- fazer função para aumentar a linha do textarea

  let minRows = 1;
  let maxRows = 2;
  
  export function rezizeTextarea(text) {
    if (text.scrollTop == 0) text.scrollTop = 1;
    while (text.scrollTop == 0) {
      if (text.rows > minRows) text.rows--;
      else break;
      text.scrollTop = 1;
      if (text.rows < maxRows) text.style.overflowY = 'hidden';
      if (text.scrollTop > 0) {
        text.rows++;
        break;
      }
    }
    while (text.scrollTop > 0) {
      if (text.rows < maxRows) {
        text.rows++;
        if (text.scrollTop == 0) text.scrollTop = 1;
      } else {
        text.style.overflowY = 'auto';
        break;
      }
    }
  }


- fazer função que verifique o tamanho do container header e atribua o tamanho para o modal menu --> class="modal-dropdown"

___________________________________________________________

FEITO:
  Posts (Criar e Mostrar na Tela):  
    - Criação dos post que são guardados no Firestore
    - Recebendo os dados dos posts que estão na coleção de nome posts do Firestore